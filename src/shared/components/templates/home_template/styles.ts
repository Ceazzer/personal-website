import tw from 'tailwind-styled-components';

export const HomeTemplateContainer = tw.div`
    relative
    flex
    flex-row
    justify-center
    items-center
    min-w-full
    min-h-[800px]
    bg-gray-100
`;