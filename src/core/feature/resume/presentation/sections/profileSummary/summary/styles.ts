import tw from "tailwind-styled-components";

export const StyledSummaryContainer = tw.div`
    relative
    flex
    flex-col
    w-full
    max-w-[480px]
    min-w-[300px]
`;

export const StyledSummaryHeader = tw.div`
    relative
    flex
    w-full
    justify-center
`;

export const StyledSummaryContent = tw.div`
    relative
    flex
    w-full
    justify-center
`;

export const StyledSummaryButtonContainer = tw.div`
    relative
    flex
    w-full
    justify-center
`;